# TODO

## Immediate Actions
* Create the centralized `maestro` orchestration loop in all 5 languages to instantiate and route data between the newly ported agents (Aider, Claude, Gemini, Q, Goose, etc).
* Address legacy Vitest mock issues once architectural foundations are solidified.

## Short-term Features
* Deepen C# and Java async/await parity for the stream-json logic and TUI hooks.
* Finalize bridging the WebUI to invoke these multi-language core services directly over IPC.
